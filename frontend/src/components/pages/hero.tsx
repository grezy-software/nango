"use server"

export default async function HeroHeader() {
  return (
    <section
      className="container flex flex-col pt-10 text-start md:pt-20 lg:items-start lg:gap-0"
      id="hero"
    ></section>
  )
}
